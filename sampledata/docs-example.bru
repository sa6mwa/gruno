meta {
  name: Docs Example
  type: http
  seq: 14
}

get {
  url: {{baseUrl}}/docs
  auth: none
}

docs {
  # API Docs
  This request fetches a markdown description. Not executed further.
}

tests {
  test("200 ok", function() { expect(res.status).to.equal(200); });
}
