meta {
  name: Gate In
  type: http
  seq: 3
  tags: ["shipping"]
}

post {
  url: {{baseUrl}}/shipments/{{shipmentId}}/gate-in
  body: json
  auth: none
}

headers {
  X-API-Key: {{apiKey}}
  Content-Type: application/json
}

body:json {
  {
    "container_id": "{{containerId}}"
  }
}

tests {
  test("200 ok", function() { expect(res.status).to.equal(200); });
  test("status set", function() { expect(res.body.status).to.equal('in-yard'); });
}
