meta {
  name: Compat GraphQL vars
  type: http
  seq: 47
  tags: [gruno, graphql]
}

post {
  url: {{graphqlUrl}}
  body: graphql
}

headers {
  Content-Type: application/json
}

body:graphql {
  query countryByCode($code: ID!) {
    country(code: $code) {
      code
      name
    }
  }
}

body:graphql:vars {
  {
    "code": "NO"
  }
}

tests {
  test("graphql vars work", function() {
    expect(res.status).to.equal(200);
    expect(res.body.data.country.code).to.equal('NO');
  });
}
