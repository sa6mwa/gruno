meta {
  name: Compat Bearer Auth
  type: http
  seq: 48
  tags: [gruno, auth]
}

get {
  url: {{baseUrl}}/bearer
}

headers {
  Authorization: Bearer demo-token
}

tests {
  test("bearer authorized", function() {
    expect(res.status).to.equal(200);
    expect(res.body.auth).to.equal('bearer');
  });
}
