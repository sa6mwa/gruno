meta {
  name: Compat XML Body
  type: http
  seq: 43
  tags: [gruno, bodies]
}

post {
  url: {{baseUrl}}/echo
}

headers {
  Content-Type: application/xml
}

body:xml {
  <note><to>John</to><from>Bruno</from></note>
}

tests {
  test("xml accepted", function() {
    expect(res.status).to.equal(200);
    expect(res.body.headers['content-type']).to.include('xml');
  });
}
