meta {
  name: Get root
  type: http
  seq: 1
  tags: [
    smoke
  ]
}

get {
  url: {{baseUrl}}/get
}

headers {
  Accept: application/json
}

body:test {
  expect(res.status).to.equal(200);
  expect(res.body).to.be.an('object');
}
