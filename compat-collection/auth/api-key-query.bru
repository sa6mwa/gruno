meta {
  name: API key query
  type: http
  seq: 33
}

get {
  url: {{baseUrl}}/anything
}

params:query {
  api_key: {{apiKey}}
}

body:test {
  expect(res.status).to.equal(200);
  expect(res.body.args.api_key).to.equal('{{apiKey}}');
}
