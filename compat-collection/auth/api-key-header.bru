meta {
  name: API key header
  type: http
  seq: 32
}

get {
  url: {{baseUrl}}/headers
}

headers {
  X-API-Key: {{apiKey}}
}

body:test {
  expect(res.status).to.equal(200);
  expect(res.body.headers['X-API-Key']).to.equal('{{apiKey}}');
}
