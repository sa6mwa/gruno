meta {
  name: Basic auth
  type: http
  seq: 30
}

get {
  url: https://httpbin.org/basic-auth/{{basicUser}}/{{basicPass}}
}

body:test {
  expect(res.status).to.equal(200);
  expect(res.body.authenticated).to.equal(true);
}
