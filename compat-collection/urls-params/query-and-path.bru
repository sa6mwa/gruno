meta {
  name: Query and path params
  type: http
  seq: 10
}

get {
  url: {{baseUrl}}/anything/:userId
}

params:path {
  userId: 42
}

params:query {
  foo: bar
  limit: 10
}

body:test {
  expect(res.status).to.equal(200);
  expect(res.body.url).to.contain("foo=bar");
  expect(res.body.url).to.contain("userId");
}
