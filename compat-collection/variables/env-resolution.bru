meta {
  name: Env resolution
  type: http
  seq: 40
}

get {
  url: {{baseUrl}}/anything
}

headers {
  X-From-Env: {{apiKey}}
}

body:test {
  expect(res.status).to.equal(200);
  expect(res.body.headers['X-From-Env']).to.exist;
}
